import React from 'react'

interface CardProps {
  name: string
  github: string
  live: string
}

const Card: React.FC<CardProps> = ({ name, github, live }) => {
  return (<>

  </>)
}

export default Card
